---
title: "タプルから値の取得【Swift】"
createAt: "2020-04-17 00:00"
updateAt: null
tags: ["Swift"]
---

# ◾️タプルから値の取得

タプルから値を取得する方法は複数ある。

以下のコードはインデックス番号を指定してタプルの値を取得する方法である。
※注意点：1つめの要素のインデックス番号は0から始まる。

（例）

```swift
let tap:(String, Int, Double) = ("こんにちは", 1, 170.0)
print(tap.0)
```

以下のコードはタプルの値につけたラベルを指定して取得する方法である。

[型推論]
（例）

```swift
let tap = (name:"名前", age:99, height:300.5)
print(tap.name)
```

[データ型指定]
（例）

```swift
let tap:(name:String, age:Int, height:Double) = ("名前", 99, 300.5)
print(tap.name)
```
