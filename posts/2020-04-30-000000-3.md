---
title: "Wordpressの環境構築をする方法（VPS使用）"
createAt: "2020-04-30 00:00"
updateAt: null
tags: []
---

# 以下の下準備を行いましょう。

- VPSを契約する。 （以下など）さくらVPS（[https://vps.sakura.ad.jp/](https://vps.sakura.ad.jp/)）
- ConoHa VPS（[https://www.conoha.jp/?btn_id=header_top](https://www.conoha.jp/?btn_id=header_top)）

</li><li>CentOS7をインストールする。- スタートアップスクリプトをLAMPを選択してインストールする。

</li></ul>

# 以下の手順でWordpressをインストールする。

- ターミナルを起動し、SSH（以下のコマンド）でサーバーにアクセスする。

```
ssh root@[IP]
```

※上記の「[IP]」には自身のサーバーのIPを代入すること（以下のようになる）

```
ssh root@111.222.33.44
```

<hr class="wp-block-separator"/>

- mariadb（MySQL）をインストール

```
yum install mariadb mariadb-server
```

- DBの自動起動設定

```
systemctl enable mariadb.service
```

- DB起動

```
systemctl start mariadb.service
```

- MySQLにログイン

```sql
mysql -u root
```

- DBを作成

```sql
create database wordpress;
```

- rootのパスワードを設定

```sql
update mysql.user set password=password('password') where user = 'root';
```

- 設定を反映

```sql
flush privileges;
```

- MySQLログアウト

```sql
exit;
```

<hr class="wp-block-separator"/>

- PHPをインストール（LAMPでインストールされているが、足りないモジュールがあるため）

```
yum -y install php
```

- ファイアーウォールを起動（CentOS7起動時には停止されている）

```
systemctl start firewalld
```

- ポート８０を開放

```
firewall-cmd --add-port=80/tcp --zone=public --permanent
```

- ファイアーウォールの再起動

```
firewall-cmd --reload
```

- Apacheの起動

```
systemctl start httpd
```

- Apacheのデーモン化（サーバー起動時に、自動起動するように設定）

```
systemctl enable httpd
```

- Apacheのルートに移動

```
cd /var/www/html/
```

- Wordpressをインストール（Zipをダウンロード）

```
wget https://ja.wordpress.org/wordpress-4.4.2-ja.zip
```

- Wordpressを解凍

```
unzip wordpress-4.4.2-ja.zip
```

- 権限をApacheで設定（「wordpress」ディレクトリにApacheでアクセスできるようにする）

```
chown -R apache. wordpress
```

- Apacheの再起動

```
systemctl restart httpd
```

<hr class="wp-block-separator"/>

- 以下のURLにアクセスして、Wordpressの手順に沿ってインストールする。http://[IP]/wordpress/
