---
title: "Sign In with Appleを実装する方法【Swift】"
createAt: "2020-05-08 00:00"
updateAt: null
tags: ["Swift"]
---

## ■はじめに

[Apple](http://d.hatena.ne.jp/keyword/Apple)から、[Google](http://d.hatena.ne.jp/keyword/Google)ログインなどのソーシャルログインを追加する場合には[Apple](http://d.hatena.ne.jp/keyword/Apple)のソーシャルログインを追加するようにレビュー[ガイドライン](http://d.hatena.ne.jp/keyword/%A5%AC%A5%A4%A5%C9%A5%E9%A5%A4%A5%F3)が更新されました。

そのため、ここでは、[Apple](http://d.hatena.ne.jp/keyword/Apple)のソーシャルログイン（[Sign](http://d.hatena.ne.jp/keyword/Sign) In with [Apple](http://d.hatena.ne.jp/keyword/Apple)）の実装方法を記載します。

このログインはiOS13から利用できますので、ターゲットバージョンを13以上にしてください。

## ■手順

プロジェクトを作成し、「Capability」で「[Sign](http://d.hatena.ne.jp/keyword/Sign) In with [Apple](http://d.hatena.ne.jp/keyword/Apple)」を追加する。

![](https://developer-kuro.com/develop/wp-content/uploads/2020/02/スクリーンショット-2020-02-18-11.30.25-300x76.png)

![](https://developer-kuro.com/develop/wp-content/uploads/2020/02/スクリーンショット-2020-02-18-11.30.46-300x219.png)

ViewControllerに「AuthenticationServices」のインポートを追加する。

```swift
import AuthenticationServices
```

[Delegate](http://d.hatena.ne.jp/keyword/Delegate)に「ASAuthorizationControllerDelegate」を追加する。

```swift
class ViewController: UIViewController, ASAuthorizationControllerDelegate {
```

「viewDidLoad」で「[Sign](http://d.hatena.ne.jp/keyword/Sign) In」のボタンを作成する。

```swift
override func viewDidLoad() {
    super.viewDidLoad()
    self.view.backgroundColor = .white

    let authorizationButton = ASAuthorizationAppleIDButton()
    authorizationButton.frame = CGRect(x: 10, y: 100, width: UIScreen.main.bounds.size.width - 20, height: 50)
    authorizationButton.addTarget(self, action: #selector(authorizationAppleID), for: .touchUpInside)
    authorizationButton.cornerRadius = 10
    self.view.addSubview(authorizationButton)
}
```

ボタン押下時の処理を追加する。

```swift
@objc func authorizationAppleID() {
    let appleIDProvider = ASAuthorizationAppleIDProvider()
    let request = appleIDProvider.createRequest()
    request.requestedScopes = [.fullName, .email]

    let authorizationController = ASAuthorizationController(authorizationRequests: [request])
    authorizationController.delegate = self
    authorizationController.performRequests()
}
```

[Delegate](http://d.hatena.ne.jp/keyword/Delegate)のメソッドを追加する。

```swift
func authorizationController(controller: ASAuthorizationController, didCompleteWithAuthorization authorization: ASAuthorization) {
    if let appleIDCredential = authorization.credential as? ASAuthorizationAppleIDCredential {
        print(appleIDCredential.user)
        print(appleIDCredential.fullName ?? "")
        print(appleIDCredential.email ?? "")
    }
}

func authorizationController(controller: ASAuthorizationController, didCompleteWithError error: Error) {
    // エラー処理
}
```

これで、実装が完了しました。

ビルドして確認してみてください。

[ソースコード](http://d.hatena.ne.jp/keyword/%A5%BD%A1%BC%A5%B9%A5%B3%A1%BC%A5%C9)の全量は以下です。

```swift
import UIKit
import AuthenticationServices

class ViewController: UIViewController, ASAuthorizationControllerDelegate {

    override func viewDidLoad() {
        super.viewDidLoad()
        self.view.backgroundColor = .white

        let authorizationButton = ASAuthorizationAppleIDButton()
        authorizationButton.frame = CGRect(x: 10, y: 100, width: UIScreen.main.bounds.size.width - 20, height: 50)
        authorizationButton.addTarget(self, action: #selector(authorizationAppleID), for: .touchUpInside)
        authorizationButton.cornerRadius = 10
        self.view.addSubview(authorizationButton)
    }

    @objc func authorizationAppleID() {
        let appleIDProvider = ASAuthorizationAppleIDProvider()
        let request = appleIDProvider.createRequest()
        request.requestedScopes = [.fullName, .email]

        let authorizationController = ASAuthorizationController(authorizationRequests: [request])
        authorizationController.delegate = self
        authorizationController.performRequests()
    }

    func authorizationController(controller: ASAuthorizationController, didCompleteWithAuthorization authorization: ASAuthorization) {
        if let appleIDCredential = authorization.credential as? ASAuthorizationAppleIDCredential {
            print(appleIDCredential.user)
            print(appleIDCredential.fullName ?? "")
            print(appleIDCredential.email ?? "")
        }
    }

    func authorizationController(controller: ASAuthorizationController, didCompleteWithError error: Error) {
        // エラー処理
    }
}
```
