---
title: "CentOS7.9にMariaDBのインストール"
createAt: "2022-08-02 10:00"
updateAt: null
tags: ["CentOS7", "MySQL"]
---

# MariaDBのインストール

以下のコマンドで設定ファイルを開く

```
vi /etc/yum.repos.d/MariaDB.repo
```

「`MariaDB.repo`」には、以下の内容を記載する。

```
[mariadb]
name = MariaDB
baseurl = http://yum.mariadb.org/10.3/centos7-amd64
gpgkey=https://yum.mariadb.org/RPM-GPG-KEY-MariaDB
gpgcheck=1
```

以下のコマンドでMariaDBをインストールする。

```
yum install MariaDB-server MariaDB-client
```

以下のコマンドでサーバー起動、再起動時の自動起動設定を行う。

```
systemctl start mariadb
systemctl enable mariadb
```

# MariaDBの設定変更

## セキュリティの設定

以下のコマンドで、rootのパスワード変更など、セキュリティ的に必要な設定を行う。

```
mysql_secure_installation
```

## 文字コード設定

以下のコマンドで設定ファイルを開く。

```
vi /etc/my.cnf.d/client.cnf
```

「`client.cnf`」に以下の内容を記載する。

```
[client-mariadb]
default-character-set=utf8mb4
```

以下のコマンドでMariaDBを再起動する。

```
systemctl restart mariadb
```
