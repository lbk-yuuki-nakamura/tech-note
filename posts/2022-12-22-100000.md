---
title: "Swiftのエラー「Terminating app due to uncaught exception 'NSInternalInconsistencyException', reason: 'unable to dequeue a cell with identifier cell - must register a nib or a class for the identifier or connect a prototype cell in a storyboard'」の対応方法"
createAt: "2022-12-22 10:00"
updateAt: null
tags: ["Swift", "Objective-C", "iOS"]
---

# エラー

StoryboardでTableViewを作成すると、アプリ起動時に以下の様なエラーが表示されることがあります。

```sh
Terminating app due to uncaught exception 'NSInternalInconsistencyException', reason: 'unable to dequeue a cell with identifier cell - must register a nib or a class for the identifier or connect a prototype cell in a storyboard'
```

# 原因

TableViewのCellに対して、Identifierが設定されていないことが原因です。

# 対応方法

TableViewのCellに対して、Identifierに任意の文字列を設定することで、エラーが解消されます。

![](/images/2022-12-22-100000/20221215112646.png)
