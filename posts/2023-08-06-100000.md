---
title: "ts-patternライブラリのmatchでnullかundefinedのチェックをする方法　TypeScript"
createAt: "2023-08-06 10:00"
updateAt: null
tags: ["React", "TypeScript", "ts-pattern"]
---

# ts-pattern

ts-patternは型比較に使用されるライブラリです。

https://github.com/gvergnaud/ts-pattern

# null、undefinedの比較

以下のように、「P.nullish」にてnullかundefinedの場合の処理を行っています。

```JavaScript
import { P, match } from 'ts-pattern';

const data: string | undefined | null = "Hello";

const result = match(data)
  .with(P.nullish, () => {
    return "null or undefined";
  })
  .otherwise((h) => {
    return h;
  });

console.log(result); // Output: Hello
```
