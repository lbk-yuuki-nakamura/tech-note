---
title: "スプレッド構文とは　JavaScript"
createAt: "2023-08-10 10:00"
updateAt: null
tags: ["JavaScript"]
---

# スプレッド構文とは

スプレッド構文は、配列やオブジェクトを展開して個々の要素やプロパティに分解するための便利な構文です。

# 配列のスプレッド構文

以下のようにスプレッド構文を使用して配列をコピーすると全く別物の配列として扱うことができます。

```JavaScript
const array = ['aa', 'bb', 'cc'];

// スプレッド構文を使用して配列を展開
const newArray = [...array];

console.log(newArray);
```

# オブジェクトのスプレッド構文

以下のようにスプレッド構文を使用してオブジェクトをコピーすると全く別物のオブジェクトとして扱うことができます。

```JavaScript
const obj = { name: 'John', age: 30 };

// オブジェクトのスプレッド構文を使用して新しいオブジェクトを作成
const newObj = { ...obj };

console.log(newObj);
```
